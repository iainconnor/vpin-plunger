// Package catalog provides xlsx download, parsing, and fuzzy matching against
// the virtual pinball community catalog. Implementation is delivered in Phase 3
// plans 04 through 06.
package catalog

import (
	// Anchor import so go mod tidy retains the dependency before implementation.
	_ "github.com/xuri/excelize/v2"
)
